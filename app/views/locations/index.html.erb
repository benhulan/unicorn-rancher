<h1>Where my Unicorns?</h1>

<% @locations.each do | location | %>
    <h3>Unicorns in the <%= link_to location.name, location_path(location) %>:</h3>
    <ul>
        <% location.unicorns.each do | unicorn | %>
           <li><%= unicorn.name  %></li>
        <% end %>
    </ul>
<% end %>